# 计算模型审计与修正记录

日期：2026-08-03

## 结论

原模型框架可保留，但旧结果不能作为监管测算依据。已发现并修正会显著改变结果的公式错误、业务分类错误和输入语义错误；同时增加公司经营范围约束。修订后的完整口径见 `calculation.md`，回归样例见 `test_cases.md`。

## 已修正问题

| 级别 | 原问题 | 修正结果 |
|---|---|---|
| 严重 | LCR/NSFR 对卖出场外期权使用压力规模或 `S_short`，而不是期末名义金额 | 分别改为 `N×20%`、`N×12%` |
| 严重 | 卖出场内期权先算 `Delta×15%` 后又乘 LCR 20%/NSFR 12%，重复折算 | `Delta×15%` 作为最终填列额 |
| 严重 | LCR 直接在线性净流出基数上相加，遗漏现金流入不超过流出 75% 的上限 | 保存毛流出、毛流入并重算前后净流出 |
| 严重 | 用首日净现金流近似资本杠杆率分母，现金买资产会错误降低总资产 | 改为表内资产与附件 2 表外项目逐项计算 |
| 严重 | ETF 按股票 8%/25% 分类 | 改为权益类指数基金 5%/其他权益基金 10% |
| 严重 | 非全额保证金境内个股互换处罚和信用风险不完整 | 市场风险加倍，另计 `N×5%` 信用风险；对冲不减免信用风险 |
| 严重 | 固定收益凭证一旦配置对冲，相关头寸仍被整体归零 | 凭证本体为零，对冲头寸独立计提 |
| 严重 | 通过是否填写压力损失推断收益凭证是否含内嵌期权 | 增加显式结构标识及内嵌期权名义金额 |
| 严重 | 同一标的命中后跳过其余对冲工具相关性检查 | 每项不同标的均需通过一年相关性 95% 条件 |
| 严重 | 组合中只要有一项填写代码，其他缺少代码的工具会被过滤并可能误判有效 | 每一项对冲工具都必须填写标的代码，缺一即失败 |
| 严重 | 有效对冲比例的符号和分母处理不稳健 | 使用多空绝对 Delta 比检查 0.8—1.25，缺失 Delta 不通过 |
| 严重 | 收益互换客户保证金随多空方向变号 | 明确字段为“向客户收取”，始终为正流入 |
| 严重 | 场外期权保证金直接按交易所期货期权保证金 100% 扣净资本 | 仅扣交易所已占用保证金；场外款项等待会计科目映射 |
| 高 | ETF 对冲权益互换仍计入 HQLA | 按附件 4 注释排除 |
| 高 | 合格股票/宽基 ETF 的 HQLA 15% 上限未实现 | 增加公司原始权益 HQLA 基数并执行非线性上限 |
| 高 | 私募基金统一按 25%，遗漏一对一单一产品及穿透孰严 | 集合 25%；单一 50%，有穿透时孰严 |
| 高 | 私募基金 NSFR 按普通证券处理 | 缺少更具体证明时按其他资产 50%/75%/100%，期限不明 100% |
| 高 | 私募基金默认 Delta=申购额并可认定有效对冲 | 必须输入经核验组合 Delta，否则不认定有效对冲 |
| 高 | 场外背对背不区分互换与期权 | 增加结构、方向、Delta、压力损失并分别计算 |
| 高 | 期权 Delta 在部分路径重复乘方向/call-put 符号 | 统一为带头寸方向的 Delta |
| 高 | 收益凭证和互换收到资金无条件增加 ASF | 仅符合不可提前偿还及期限条件时计入；互换默认不计 |
| 高 | 平盘年化费用混入首日现金流 | 首日只计预付金，年化费用从预计收入扣除 |
| 高 | 分类评价系数标签错误且资产总额系数未独立 | 分别采用附件 3 与附件 2 的两套系数 |
| 高 | 个股对冲、海外业务没有阻断 | 按公司约束直接报错；ETF 保留并区分宽基 |
| 中 | 收入/占用分母为零时输出 999 | 改为“不适用” |
| 中 | 操作风险资本准备完全遗漏 | 增加 `净收入×识别权重×18%×分类系数` 的预计影响 |
| 中 | 自营权益类证券及其衍生品规模未输出 | 增加增量及占净资本比例 |
| 中 | 裸指数代码 `000300` 被识别为深市证券 | 对模型常用、明确的上证/中证指数做显式 `.SH` 映射；保留歧义提示 |
| 安全 | 测试脚本含明文第三方登录凭据且会真实登录 | 移除凭据和外部登录，改为离线 mock；原凭据应立即轮换 |

## 历史 Excel 的处理

该表使用了旧系数（例如指数成份股 10%、一般股票 30%、部分 HQLA 40%），不能覆盖现行规定。仍有三点可作为公司口径线索：SPV 曾按一对一单一产品和 NSFR 其他资产处理；监管回复提示互换预付金不能直接计入 ASF；权益互换对冲证券不计 HQLA。模型采用现行附件系数，并把这些线索转为需要显式输入或审慎默认的规则。

## 仍需公司确认的数据与边界

1. 场外保证金、预付金、收益凭证募集资金在总账中的具体科目、剩余期限和提前偿还权；这决定净资本及 ASF 的最终行次。
2. 收益凭证内嵌衍生品是否满足权益期权监管识别及其真实名义金额、Delta、压力损失。
3. 私募基金/SPV 是集合还是单一产品、能否穿透、资产剩余期限和经核验 Delta。
4. 公司 LCR 毛流出、毛流入和折算前权益类 HQLA 基数。缺少时虽可审慎测算，但边际结果可能偏保守。
5. 新业务收入进入近三年平均净收入的实际时点和识别权重。
6. 附件 6 其余公司级集中度/负债指标所需净资产、发行人成本、市值和总市值数据。

在以上数据未与财务、风控监管报表勾稽前，输出适合方案比较和压力测试，不应直接作为正式监管报送数。
